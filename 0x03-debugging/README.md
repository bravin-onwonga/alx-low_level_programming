0-main.c tests that the function positive_or_negative() gives the correct output when given a case of 0.

1-main.c comments out a bug that causes an infinite loop.

2-largest_number.c fixes bug causing a false output

3-print_remaining_days.c adds a day if it is a leap and returns an erro if the date is invalid ie. February 29 of a non-leap year being counted.
