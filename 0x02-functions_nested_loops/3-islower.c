#include "main.h"

/**
 *_islower - checks if c is in lowercase
 *@c : is the value being checked
 *Return: 1 if true else 0
 */

int _islower(int c);
{
	int ascii = c;

	if (ascii > 97)
	{
		return ('1');
	}
	return ('0');
}
