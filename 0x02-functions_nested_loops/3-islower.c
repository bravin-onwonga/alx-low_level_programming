#include "main.h"

/**
 *_islower - checks if c is in lowercase
 *@c : is the value being checked
 *Return: 1 if trueotherwise 0
 */

int _islower(int c);
{
	if (c >= 'a' && c <= 'z')
		return ('1');
	else
		return ('0);
}
