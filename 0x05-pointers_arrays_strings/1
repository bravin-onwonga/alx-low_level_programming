#include "main.h"

/**
  * rev_string - prints string then prints it in reverse
  *
  * @s: pointer to string
  * @i - integer
  * @j - integer
  */

void rev_string(char *s)
{
	int i, j;

	for (i = 0; s[i] != '\0'; i++)
		;
	for (j = i - 1; j >= 0; j--)
		s = s[j];
}
