#include "main.h"

/**
 *rev_string - prints a string in reverse
 *@s: pointer to string to be reversed
 *
 *Return: Nothing.
 */

void rev_string(char *s)
{
	int i;
	char c;

	for (i = 0 ; s[i] != '\0'; i++)
	{

	}
	for (i = i - 1; i >= 0; i--)
	{
		c = s[i];
	}
}
