__0-create_array.c__ - creates an array of chars, and initializes it with a specific char.

__1-strdup.c__ - returns a pointer to a newly allocated space in memory, which contains a copy of the string given as a parameter.

__2-str_concat.c__ - concatenates two strings.

__3-alloc_grid.c__ - returns a pointer to a 2 dimensional array of integers.

__4-free_grid.c__ -  frees a 2 dimensional grid previously created by the alloc_grid function.
